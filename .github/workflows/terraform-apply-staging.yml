name: "Terraform Apply - Staging"

on:
  push:
    branches: [main]
    paths:
      - "infrastructure/terraform/**"
      - ".github/workflows/terraform-apply-staging.yml"
  workflow_dispatch:

jobs:
  apply:
    name: "Terraform Apply (Staging)"
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "TODO - Terraform apply staging"
