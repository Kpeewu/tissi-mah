apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tileserver-gl
  namespace: default
  labels:
    app: tileserver-gl
    tier: tiles
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: tileserver-gl
